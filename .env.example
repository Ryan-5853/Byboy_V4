# Copy this file to `.env` in the project root and fill in only what you need.
# `.env.local` is also supported and will override `.env`.

# Local OpenAI-compatible backend
LOCAL_OPENAI_API_KEY=

# Cloud providers
DEEPSEEK_API_KEY=
OPENAI_API_KEY=

# Optional workflow UI override
WEBUI_PORT=8897
